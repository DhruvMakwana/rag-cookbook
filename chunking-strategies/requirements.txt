# Core
pypdf>=4.0
numpy>=1.26
python-dotenv>=1.0
requests>=2.31

# Embeddings — used by semantic, sentence-window scoring, small-to-big,
# adaptive, and late chunking (late chunking additionally needs `transformers`
# + `torch`, which sentence-transformers already pulls in).
sentence-transformers>=3.0
transformers>=4.40

# LLM providers — used by proposition and agentic chunking. Install
# whichever you plan to use (see .env.example). Ollama needs no SDK.
anthropic>=0.34
openai>=1.40
