# Copy this file to .env in the same folder. Never commit .env.

# Only needed if LLM_PROVIDER=anthropic or openai (default is ollama,
# which needs no key at all).
LLM_PROVIDER=ollama
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
