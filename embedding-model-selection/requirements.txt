# Core
pypdf>=4.0
numpy>=1.26
requests>=2.31
langchain-text-splitters>=0.3

# Embedding models — everything in this recipe runs fully locally, no API key.
sentence-transformers>=3.0
transformers>=4.40
FlagEmbedding>=1.3        # BGE-M3 (dense + sparse + multi-vector in one model)

# Qwen3-Embedding and BGE-M3 both need these for their tokenizers.
sentencepiece>=0.2
protobuf>=4.25
