# Core
pypdf[image]>=4.0
numpy>=1.26
requests>=2.31
python-dotenv>=1.0
langchain-text-splitters>=0.3
pillow>=10.0

# Embeddings — a dedicated text embedder, and jina-clip-v2 for the
# shared text+image space. jina-clip-v2 was picked after measuring it
# directly against 2 other candidates (see the recipe's README) — it
# genuinely outperformed both. It needs `trust_remote_code=True` (its
# model code lives in its HuggingFace repo, not in `transformers`
# itself) and a pinned `transformers` version — a newer one breaks its
# remote code (`clip_loss` was removed from
# `transformers.models.clip.modeling_clip`, which jina-clip-v2's
# custom code still imports).
sentence-transformers>=3.0
transformers==4.46.3
einops>=0.7
timm>=1.0

# LLM providers — only needed for the final generation step
anthropic>=0.34
openai>=1.40
